# Журнал изменений — Jobish

Все изменения по плану `docs/gap-analysis.md`. Каждый «блок» — отдельная ветка
от `main`; применяются выборочно через merge. Здесь фиксируются все правки,
включая ещё не слитые в `main` ветки.

Формат: [тип] коротко — детали. Типы по Conventional Commits.

## Исправления (после merge)
- **fix(roadmap)** `bd59445`: `suppressHydrationWarning` на `<body>` (расширения браузера
  добавляют атрибуты типа `data-gptw` до гидратации — это не наша рассинхронизация);
  кнопка **«Обновить»** и действие `refresh_roadmap` — неразрушающий бэкфилл материалов
  (вкл. soft-курсы), бейджей hard/soft, описаний этапов и часов в существующую карту
  без потери прогресса. Старые карты (созданные до Блока B) так подтягивают soft-ссылки.
  Важно: после смены схемы **dev-сервер нужно перезапустить** (иначе старый Prisma-клиент
  не знает новых полей).

## Слито в main (merge-коммиты c9b7c82, 372698e, 58c4c77)
Блоки A/B/C собраны в `main` без конфликтов. Ниже — что в каждом.

### Блок A — профориентационный тест · ветка `feat/orientation-test`
- **feat(ft-2):** банк профтеста расширен с 6 до **30 вопросов** (нижняя граница ФТ-2.1: 30–60).
- Методология: адаптация модели Дж. Голланда (**RIASEC**) + публичная методика
  **O*NET Interest Profiler**; показывается сноской в UI.
- Демо-кнопка **«Пропустить опрос»** (`randomAnswers`) — случайные ответы для быстрого превью результата.
- Динамический счётчик вопросов на странице.
- Тесты: +6 unit (банк, `randomAnswers`).
- Файлы: `src/lib/orientation.ts`, `src/components/OrientationQuiz.tsx`,
  `src/app/(app)/orientation/page.tsx`, `tests/unit/orientation.test.ts`.
- Коммит: `4deb9ee`.

### Блок B — карта развития · ветка `feat/roadmap-rework`
- **feat(ft-4):** карта стала развёрнутее и понятнее.
  - Осмысленные **описания этапов** (навыки + результат по грейду).
  - Бейджи **hard/soft** и пояснение «зачем» к каждому навыку.
  - **Честные метаданные**: тип/рейтинг материала только если он есть; реалистичные
    часы по типу/грейду вместо одинаковых фейков.
  - **Легенда статусов**, подсветка «**Следующий шаг**», прогресс-бар по этапу.
  - Схема: `RoadmapStage.description`, `RoadmapStep.skillType`.
  - Новый чистый модуль `src/lib/roadmap-content.ts` + 6 unit-тестов.
  - Коммит: `17b68dd`.
- **feat(ft-5):** реальные **курсы по soft-skills**.
  - Карта `SOFT_MATERIALS`: 18 soft-навыков по 6 профессиям получили проверенную
    ссылку (Coursera free audit / Stepik free / Michigan Online / Storytelling with Data).
  - Подмешиваются в материалы профессии в `prisma/seed.ts`; в карте у soft-навыков
    теперь курс вместо «материал не задан».
  - Коммит: `906229d`.

### Блок C — сводка рынка + hh.ru API · ветка `feat/market-hh-api`
- **feat(ft-3.1):** медиана и перцентили зарплат с разрезом по городу.
  - Новый чистый модуль `src/lib/salary-stats.ts` (`percentile`/`salarySummary`/
    `modelSalarySample`) + 9 unit-тестов.
  - `src/lib/hh.ts` — клиент **публичного** hh.ru `/vacancies` (токен не нужен; важен
    User-Agent), под флагом `HH_ENABLED`, с мягким фолбэком (null) при недоступности.
  - Роут `/api/market/salary` — live-статистика hh.ru или оценка по сид-данным.
  - Компонент `SalaryStats`: селектор города (Россия/Москва/СПб) + медиана + p25/p75/p90
    + подпись источника; встроен в блок «Аналитика рынка» профессии.
  - Секреты/настройки hh.ru — в `.env` (gitignore); `.env.example` обновлён.
  - Примечание: hh.ru блокирует серверные IP — из песочницы отрабатывает фолбэк;
    на доступной сети возвращаются live-данные.
  - Коммит: `724ee7f`.

## В main
- **docs:** `docs/gap-analysis.md` (сверка реализовано vs документ) + фазовый план. Коммит `fe49152`.
- **docs:** этот журнал `CHANGELOG.md` (`3773d76`).

## Запланировано (по плану)
- Все три приоритетных блока (A, B, C) реализованы. Дальше по `docs/gap-analysis.md`:
  SSO-задел (ФТ-1.1), админка (US17), i18n RU/EN.
